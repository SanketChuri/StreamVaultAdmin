@model StreamVaultAdmin.Models.ContentItem
@using StreamVaultAdmin.Models

@{
    ViewData["Title"] = "Edit";
    string type = Model.ContentType;
}

<h2>Edit @type</h2>

<a href="/Catalogue">Back to Catalogue</a>

<br /><br />

<form method="post" action="/Catalogue/Edit/@Model.Id">

    <h4>General</h4>

    <p>
        <label>Title</label><br />
        <input type="text" name="Title" value="@Model.Title" required />
    </p>

    <p>
        <label>Description</label><br />
        <textarea name="Description" rows="3" cols="50" required>@Model.Description</textarea>
    </p>

    <p>
        <label>Release Date</label><br />
        <input type="date" name="ReleaseDate"
               value="@Model.ReleaseDate.ToString("yyyy-MM-dd")" required />
    </p>

    <p>
        <label>Age Rating</label><br />
        <select name="AgeRating" required>
            <option value="">Select...</option>
            @foreach (var rating in new[] { "U", "PG", "12", "12A", "15", "18" })
            {
                @if (Model.AgeRating == rating)
                {
                    <option selected>@rating</option>
                }
                else
                {
                    <option>@rating</option>
                }
            }
        </select>
    </p>

    <p>
        <label>Genre</label><br />
        <input type="text" name="Genre" value="@Model.Genre" required />
    </p>

    <h4>@type Details</h4>

    @if (Model is Movie movie)
    {
        <p>
            <label>Duration (minutes)</label><br />
            <input type="number" name="Duration" value="@movie.Duration" min="1" required />
        </p>
        <p>
            <label>Director</label><br />
            <input type="text" name="Director" value="@movie.Director" required />
        </p>
    }
    else if (Model is Series series)
    {
        <p>
            <label>Number of Seasons</label><br />
            <input type="number" name="NumberOfSeasons" value="@series.NumberOfSeasons" min="1" required />
        </p>
        <p>
            <label>Total Episodes</label><br />
            <input type="number" name="TotalEpisodes" value="@series.TotalEpisodes" min="1" required />
        </p>
    }
    else if (Model is Audiobook audiobook)
    {
        <p>
            <label>Author</label><br />
            <input type="text" name="Author" value="@audiobook.Author" required />
        </p>
        <p>
            <label>Narrator</label><br />
            <input type="text" name="Narrator" value="@audiobook.Narrator" required />
        </p>
        <p>
            <label>Duration (minutes)</label><br />
            <input type="number" name="Duration" value="@audiobook.Duration" min="1" required />
        </p>
    }
    else if (Model is MusicAlbum album)
    {
        <p>
            <label>Artist</label><br />
            <input type="text" name="Artist" value="@album.Artist" required />
        </p>
        <p>
            <label>Track Count</label><br />
            <input type="number" name="TrackCount" value="@album.TrackCount" min="1" required />
        </p>
        <p>
            <label>Record Label</label><br />
            <input type="text" name="RecordLabel" value="@album.RecordLabel" required />
        </p>
    }

    <p>
        <button type="submit">Update</button>
        <a href="/Catalogue">Cancel</a>
    </p>

</form>